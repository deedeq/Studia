#include<iostream>

using namespace std;

int main()
{
	int a,b,c,d,e,f;
	
	cin>>a>>b>>c;
	cin>>d>>e>>f;
	
	if(a<d || (a==d && b<e) || (a==d && b==e && c<f))
	{
		cout<<a<<" "<<b<<" "<<c;
	}
	else
	{
		cout<<d<<" "<<e<<" "<<f;
	}
	
	return 0;
}
